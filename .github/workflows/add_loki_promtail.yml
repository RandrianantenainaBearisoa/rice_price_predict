name: Add loki and promtail to prometheus monitoring.

on:
  workflow_dispatch:

jobs:
  Add_loki_n_promtail:
    runs-on: vm-prod
    steps:
      - name: Download promtail and loki Config files
        run: |
          rm $HOME/loki-promtail/* -f
          cd $HOME/loki-promtail
          wget https://github.com/RandrianantenainaBearisoa/rice_price_predict/raw/refs/heads/main/loki-config.yaml -O loki-config.yaml
          wget https://github.com/RandrianantenainaBearisoa/rice_price_predict/raw/refs/heads/main/promtail-config.yaml -O promtail-config.yaml
          wget https://github.com/RandrianantenainaBearisoa/rice_price_predict/raw/refs/heads/main/docker-compose-loki-promtail.yml -O docker-compose.yml
      - name: Run docker compose
        run: |
          cd $HOME/loki-promtail
          docker compose up -d